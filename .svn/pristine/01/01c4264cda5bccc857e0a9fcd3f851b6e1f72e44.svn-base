"use strict";
cc._RF.push(module, '5fcb1YhOTJADJxplhnYTTcy', 'AdButton');
// Script/gamecore/xiaoyaoji/AdButton.ts

Object.defineProperty(exports, "__esModule", { value: true });
var GameManager_1 = require("../managers/GameManager");
var XYJAPI_1 = require("./XYJAPI");
var WXCore_1 = require("../wechat/WXCore");
// Learn TypeScript:
//  - [Chinese] http://docs.cocos.com/creator/manual/zh/scripting/typescript.html
//  - [English] http://www.cocos2d-x.org/docs/creator/manual/en/scripting/typescript.html
// Learn Attribute:
//  - [Chinese] http://docs.cocos.com/creator/manual/zh/scripting/reference/attributes.html
//  - [English] http://www.cocos2d-x.org/docs/creator/manual/en/scripting/reference/attributes.html
// Learn life-cycle callbacks:
//  - [Chinese] http://docs.cocos.com/creator/manual/zh/scripting/life-cycle-callbacks.html
//  - [English] http://www.cocos2d-x.org/docs/creator/manual/en/scripting/life-cycle-callbacks.html
var _a = cc._decorator, ccclass = _a.ccclass, property = _a.property;
/**
 * 广告按钮
 */
var AdButton = /** @class */ (function (_super) {
    __extends(AdButton, _super);
    function AdButton() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /**
         * 动画间隔
         */
        _this.effectDelay = 3;
        return _this;
    }
    AdButton.prototype.start = function () {
        this.node.on(cc.Node.EventType.TOUCH_END, this.tapHandler, this);
        this.showEffect();
        this.schedule(this.getAdData, 2, 1);
        //默认不可见
        // this.node.active = false;
        this.node.opacity = 0;
    };
    AdButton.prototype.getAdData = function () {
        cc.info("get ad data");
        if (this._adData)
            return;
        //请求数据
        var script = this;
        XYJAPI_1.default.getAdImageData(function (data) {
            if (data && data["imageurl"]) {
                script._adData = data;
                //script.node.active = true;
                script.node.opacity = 255;
            }
        });
        this.schedule(this.getAdData, 3, 1);
    };
    // update (dt) {}
    /**
     * 广告按钮点击
     * @param e
     */
    AdButton.prototype.tapHandler = function (e) {
        if (this.node.opacity < 10)
            return;
        GameManager_1.default.soundsManager.playTapSound();
        //预览图片
        if (this._adData) {
            WXCore_1.default.previewImage([this._adData["imageurl"]]);
        }
    };
    /**
     * 播放效果
     */
    AdButton.prototype.showEffect = function () {
        this.node.stopAllActions();
        if (this._adData) {
            if (Math.random() > 0.5) {
                this.effectZoom();
            }
            else {
                this.effectShake();
            }
        }
        this.schedule(this.showEffect, this.effectDelay + Math.random(), 1);
    };
    /**
     * 晃动
     */
    AdButton.prototype.effectShake = function () {
        var toR = (40 + Math.random() * 10) * (Math.random() > 0.5 ? 1 : -1);
        // let cc.sequence(cc.rotateTo(0.5, toR));
        var act = cc.sequence(cc.rotateTo(0.5, toR), cc.rotateTo(0.5, 0).easing(cc.easeBounceOut()));
        this.node.runAction(act);
        // let t:any = TweenLite.to(this.node, 0.5, {"rotation":toR});
        // this._tweenlites.push(t);
        // t = TweenLite.to(this.node, 0.8, {
        //     "delay":0.5,
        //     "rotation":0,
        //     "ease":Elastic.easeOut
        // });
        // this._tweenlites.push(t);
    };
    /**
     * 缩放
     */
    AdButton.prototype.effectZoom = function () {
        var toS = 0.6 + Math.random() * 0.2;
        // let cc.sequence(cc.rotateTo(0.5, toR));
        var act = cc.sequence(cc.scaleTo(0.5, toS), cc.scaleTo(0.5, 1).easing(cc.easeBounceOut()));
        this.node.runAction(act);
    };
    AdButton.prototype.onDestroy = function () {
        this.node.stopAllActions();
        this.unschedule(this.showEffect);
        this.unschedule(this.getAdData);
    };
    AdButton = __decorate([
        ccclass
    ], AdButton);
    return AdButton;
}(cc.Component));
exports.default = AdButton;

cc._RF.pop();